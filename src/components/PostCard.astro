---
import type { CollectionEntry } from 'astro:content';
import { site, topicName, type TopicItem } from '../site';

interface Props {
  post: CollectionEntry<'posts'>;
  topics: TopicItem[];
}
const { post, topics } = Astro.props;
const { title, description, date, topic, pinned, cover, readTime } = post.data;
const formattedDate = new Intl.DateTimeFormat('zh-CN', { year: 'numeric', month: '2-digit', day: '2-digit' }).format(date);
---

<article
  class:list={['post-card', { 'text-cover': !cover }]}
  data-search-text={`${title} ${description} ${topicName(topics, topic)}`}
>
  <a class="card-link" href={`/articles/${post.id}/`} aria-label={`阅读：${title}`}>
    <div class="card-media">
      {cover ? <img src={cover} alt="" loading="lazy" /> : (
        <div class="generated-cover">
          <span>{topicName(topics, topic)}</span>
          <strong>{title}</strong>
          <i>{site.name} · 投资研究</i>
        </div>
      )}
      {pinned && <span class="pin-badge">置顶</span>}
    </div>
    <div class="card-body">
      <div class="card-meta">
        <span>{topicName(topics, topic)}</span>
        <span>{formattedDate}</span>
        <span>阅读时间 {readTime} 分钟</span>
      </div>
      <h2>{title}</h2>
      <p>{description}</p>
      <span class="read-more">阅读全文 <span aria-hidden="true">→</span></span>
    </div>
  </a>
</article>
